import './Loader.css';

const Loader = () => {
    return (
        <div className="logo-loader-container">
            <span className="loader"></span>
        </div>
    );
};

export default Loader;
